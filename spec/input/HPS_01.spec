นำเข้า posgateway.exchange.hps.*

อธิบาย ตัวจัดการ ดังนี้
	
	ก่อน(:แต่ละ)ให้ทำ
		ประกาศ ตัวจัดการ1 = สร้าง ตัวจัดการ()
		ประกาศ ตัวจัดการ2 = สร้าง ตัวจัดการ()		
	จบ
	
	หลัง(:ทั้งหมด)ให้ทำ
		
	จบ
	
	กำหนดให้ ควรจะตั้งค่าให้ดี โดย
		
	จบ
	
จบ